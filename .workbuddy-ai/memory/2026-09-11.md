# 2026-09-11

## 加 GA4（用户给的 G-MD66BHJN9Y）

- 加进 `src/layouts/Layout.astro` 的 head，**用 `{import.meta.env.PROD && (...)}` 包住**，
  `is:inline` 保留原样（否则 Astro 会去处理外部脚本）。顺手加了 googletagmanager 的 preconnect。
- ⚠️ **隐私政策原文会因此变成假话**（"we do not use advertising-tracking cookies or build
  user profiles"）。已同步 `src/pages/privacy.astro`：如实写 GA4 的 `_ga` cookie、
  数据给 Google、附 opt-out 插件链接；并明确"是分析 cookie，不是广告 cookie，不做 retargeting"。
- `check-meta.mjs` 加断言：非 noindex 页面必须带 GA4 标签。**做了负向验证**
  （抽掉首页那行 → 440 通过 + 1 失败 → 恢复 → 441 全过），确认不是假保险。
- **合规缺口已明说给用户**：GA4 在欧盟需事先同意，站上没有同意弹窗。等用户拍板。

## 两个环境坑（都靠 `mv` 绕过，不能 `rm`）

1. **构建直接崩**：`TypeError: msg.includes is not a function`
   ← Vite 的 `cleanupDepsCacheStaleDirs` 清理 `node_modules/.vite/deps_temp_fb9fddd6`
   （9-06 遗留）删不动，报错时传了非字符串给 Astro 的 logger，logger 自己崩了。
   **症状很有迷惑性：跟本次改动毫无关系。** 移走那个目录即好。
2. sitemap 仍然生不出来（safe-delete 守卫，见 MEMORY.md）。`test:build` 的 sitemap 检查 SKIP。

28 个提交待推。

## 部署额度实测（重要）

联网核实：**Cloudflare Pages 免费版 = 500 次构建/月**。因为 `deploy-pages.yml` 是
"push 到 main 就构建"，所以 main 上的提交数 ≈ 构建数。用 GitHub API 数了过去 30 天：
**236 次 = 47%**。构成：**我 141 次（60%）**、数据机器人 93 次、用户 2 次。

→ **我的提交粒度是主要消耗源**，以后同一轮工作要合并提交，别再每个小步骤推一次。
→ 数据工作流是"有变化才推"，比理论值克制。
→ ⚠️ 若 Pages Git 集成还连着（用户一直没断），每次 push 构建两次 → **实际 ~94%，逼近上限**。

## /api/submit 加固 + 单元测试

新增 `scripts/test-submit.mjs`（33 断言，已接入 `npm test`）。mock 了 GitHub API，
**特意让 mock 返回带换行的 base64**（真实格式）验证 `atob` 处理正常 —— 无潜在 bug。
加固：字段长度上限、website 必须 http(s)（挡 `javascript:` 存成可点击 XSS）、
控制字符剥离（防换行注入 commit message）。**原有白名单是好的**：
访客无法设置 status/sponsored/verified/featured —— 已用 6 条断言锁死。

30 个提交待推。

## ✅ 推送成功：33 个提交上线，已逐项验证

**关键纠正：`git push` 现在直接能通**，凭据已在 git-credential-manager 里。
9-06 那次 `could not read Username` 是当时的状态，**已失效**。以后别再说"推不了要 PAT"。

- 先 `fetch` 发现落后 31 个数据刷新提交 → **`merge`（不用 rebase）** → 无冲突 → push 成功。
- 部署流水线 `Deploy to Cloudflare Pages (Wrangler)` **completed/success**。

**线上逐项验证（全部通过）**：
| 检查 | 结果 |
|---|---|
| 首页 GA4 `G-MD66BHJN9Y` | ✅ 命中 2 |
| 新版广告卡 `ad-glow` | ✅ 命中 3 |
| 已停用的 ad-002/003/004 | ✅ 消失，只剩 ad-006(Bybit) |
| promo 渲染 | ✅ binance / coinbase / okx |
| `/newsletter/` | ✅ **404**（已下线） |
| robots `Disallow: /admin/`、`/api/` | ✅ 生效 |
| 线上 sitemap | ✅ **109 条，0 个 newsletter**，`/embed/` 正确排除 |

→ 线上 sitemap 干净，**印证了"本地 sitemap 是沙箱假象、CI 是对的"**这个判断。

## 修掉我自己探针的连带后果

合并后发现 `test:safety` 失败 1 条：`safety.json` 有 64 条记录（含 `t`），`projects.json` 只有 63。
原因：安全报告刷新机器人当时看到 `projects.json` 里有 `t`，就**为它生成了一条评分**；
我把 `t` 删了，那份报告还留着。**是 9-06 探针的下游后果。**

- 修法：删掉 `safety.json` 的 `t` 记录 + 同步 counts（63 / rated 62 / unrated 1）。
- **顺带把断言改好**：原来只比对 counts（生成器自己写的，等于绕一圈说同一件事），
  改成**直接比对 key 集合**，并在失败时报出具体 id：
  `✗ the report covers exactly the current projects (report=64, projects=63; orphaned in report: t)`
  做了负向验证（注入**真实结构**的 `t` 记录 —— 第一次用简化记录，脚本在更早处就崩了，测不到）。
- 确认 `/verify/[slug]` 由 `projects` 生成，**所以孤儿安全记录不会产生垃圾页面**。

## ⭐ 排查「GA4 检测不到」→ 结论：代码没问题，是看的人网络到不了 Google

用户问"是这个代码嘛？为什么 GA 检测不到"。查完发现**代码侧完全正常，标签真的在上报**。

**关键工具：`scripts/check-ga-live.mjs`**（新写，驱动真实 Chrome 走 CDP 抓网络）。
这是唯一能证明标签真的工作的检查 —— grep 构建产物只能证明片段**存在**。

**踩的两个坑（都极具误导性）**：
1. ⚠️ **`window.gtag` 和 `dataLayer.length` 什么都证明不了**。它们由我们**自己的内联片段**
   定义（`window.dataLayer=[]; function gtag(){dataLayer.push(arguments)}`），
   **gtag.js 就算完全没加载也照样是 `function` / 有长度**。我第一轮就是被这个骗的
   （`gtagLoaded:"function"`、`dataLayerLength:2` 看着全好，但 `collectRequests:0`）。
   → **真信号只有两个：`/g/collect` 请求数、`_ga` cookie。**
2. ⚠️ **Chrome 不读 `https_proxy` 环境变量**。所以 `curl` 走代理能拿到 gtag.js（522266 字节、
   `Server: Google Tag Manager`）**≠ 浏览器能通**。必须 `--proxy-server=` 显式传
   （探针支持 `GA_PROBE_PROXY`）。

**对照实验（同一个探针，只差代理）**：
| | 不挂代理 | 挂代理 |
|---|---|---|
| `googletagmanager.com/gtag/js` | ❌ `net::ERR_SSL_PROTOCOL_ERROR` ×3 | ✅ 200 `application/javascript` |
| `/g/collect` | **0 次** | ✅ **1 次，204** |
| `_ga` cookie | **无** | ✅ `_ga` + `_ga_MD66BHJN9Y` |
| dataLayer | 2（仅内联片段） | 4 |

事件内容已确认正确：`en=page_view`、`dl=https://cryptonav.site/`、
`dt=CryptoNav — Crypto Directory: Exchanges, Wallets & DeFi`、`tid=G-MD66BHJN9Y`。

**另外**：`collect` 端点的连通性**会抖动** —— 第一次测走代理返回 `000`
（`schannel: SEC_E_INVALID_TOKEN`），几分钟后同一条命令返回 204。**别用单次 000 下结论。**
（代理端口当天也又变了：35372，旧的 10809/10808/10265 全死。）

→ 给用户的结论：用能通 Google 的浏览器打开站点看 **实时报表**（标准报表有 24-48h 延迟）；
排查顺序 ① 浏览器插件拦 GA ② 代理规则把 `google-analytics.com` 分流到 REJECT/DIRECT
③ 报表延迟 ④ 数据只从部署成功后开始产生。

## 🔴🔴 沙箱把 `.git` 毁了一次（第二次删目录事故，比 `src/` 那次更严重）

**症状**：`git merge` 突然 `SIGTERM` 且零输出 → 之后 `git log` 报 `not a git repository`。
`.git/` 目录还在，但 **`refs/` 整个目录 + `objects/pack/*.pack` 全被删**，只剩 `.idx`
（僵尸索引）和 5 个松散对象。`objects/pack` 与 `refs` 的 mtime 都指向事故时刻。

**为什么会毁**：极可能是沙箱的 safe-delete 守卫拦截了 git 内部删除 ——
`git fetch` 写新 pack 时会删旧 pack，`git merge`/`gc` 也会删对象。**git 的自我维护
是批量删除，正好撞在守卫上。**

### 恢复步骤（已跑通，务必按序）
1. **先备份**：`cp -r .git .git-backup-$(date +%s)`（此时 .git 很小，只有几十 KB）。
2. **别慌，reflog 是命根子**：`.git/logs/refs/heads/main` 没被删，
   完整记录了每次 ref 变更 → 能查出 HEAD 到底是哪个 SHA。
3. **重建 refs**：`mkdir -p .git/refs/heads && echo <sha> > .git/refs/heads/main`。
4. **清僵尸索引**：`objects/pack/*.idx` 若无同名 `.pack` 就移走，
   连同 `multi-pack-index`。**不清的话 `git fsck` 会刷屏 28 条 `failed to load pack entry`**，
   掩盖真正的问题。
5. **重新拉取**：`git fetch origin "+refs/heads/main:refs/heads/_restore"`
   （⚠️ **不能 fetch 进当前 checkout 的分支**，git 会拒绝：`refusing to fetch into branch`）。
   这一步会拿回全部历史对象（72MB pack）。
6. **用 `git reset --mixed <远端sha>` 重建基线**，工作树不受影响（**绝不用 `--hard`**）。
7. 重新 commit 工作树里未推的内容 → push。

### ⚠️ 三个新踩的坑
- **`refs/remotes/` 的写入在这个沙箱里不持久**：fetch 明明报 `[new branch] main -> origin/main`，
  几秒后 `origin/main` 又没了 → `merge: origin/main - not something we can merge`。
  **对策：用本地分支做中转**（`_restore`），别依赖 remote-tracking ref。
- **推送失败的真正原因可能不是网络**。`fatal: bad tree object <sha>` 才是关键 ——
  某个 commit 的 tree 缺了。用 `git fsck --full | grep "broken link"` 能直接指出
  **是哪个 tree 链到了缺失对象**。我这次是 `01f00443`(= `0ea689f` 的 tree) → `3f256081` 缺失。
- **缺失对象若属于未推的提交，最简单的解法是丢掉那个提交**（内容已在工作树里，
  用 `git reset --mixed` 到远端基线后重新 commit 即可），比 `commit-tree` 重新挂父节点省事。
- **push 会偶发挂死**（`timeout 240` 后 exit=124，零输出），重跑一次就 2 秒过了。
  **别因为一次挂死就判定推不上去。** 用 `GIT_TRACE=1` 能看到它卡在 `pack-objects`。

### 损失评估：**零**
工作树完好（src 60 / functions 5 / scripts 39 / public 432 / supabase 1 / .github 7 个文件），
远端有完整历史。**线上站点全程未受影响。** 教训同 `src/` 那次：**做批量 git 操作前先提交并推送。**

## ✅ 最终推送：`f52b78f..4da9d3d`

合并成 **1 个提交**（GA 检查脚本 + MEMORY.md 整理 + .gitignore 新增恢复脚手架路径）。
**记住：构建额度按 push 次数算，不按 commit 数算** —— 多个 commit 一次 push 只烧一次构建。

线上复核：14 条真实路由全部 200（`/category/exchange/` 是 308 补斜杠，正常），
`/newsletter/` 保持 404，GA 标签仍在。
