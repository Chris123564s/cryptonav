# 2026-08-31

## 全站审计（用户提问："看下网站还有哪些未完善的地方"）

### 已修复并上线

1. **`/api/cg/simple/price` 502 —— 确认是部署滚动期瞬时抖动，非代码问题**
   - 直连上游带 key：`/global` 200、`/simple/price` 200（`{"bitcoin":{"usd":77809}}`）
   - 线上代理 5 个端点全部 200，`X-CG-Auth: demo-key` 生效
   - 用组件真实请求串复测：TickerBar 29 币 200、dashboard per_page=100 200
   - 教训：`/tmp` 在本沙箱不可写，curl exit 23 会造成**假阴性**。探测脚本必须写项目目录内。

2. **死链 `/category/exchanges`（8 篇 learn 文章 CTA 全中招）** — commit `c686b20`
   - `src/pages/learn/[slug].astro:130` 硬编码 `/category/exchanges`（复数），实际 id 是单数
   - 文案也错：用 `article.tags[0]`（"staking"/"pos" 等自由标签）冒充目录分类
   - 修法：给 8 篇文章加 `directoryCategory`（合法分类 id），CTA 按文章主题跳对应分类
     defi×3 / infra×2 / market×2 / analytics-trading×1
   - 注意：`article.category` 是**显示名**（"DeFi"/"Education"），供徽章和相关文章分组用，不可当 id 用

3. **`.gitignore` 补 `.dev.vars`** — commit `1946527`

### 审计方法（可复用）

爬 sitemap 全站 → 抽所有站内 `href` → 逐个验状态码。比"猜 URL 再测"可靠得多：
猜 URL 会产出大量假死链（`/project/*`、子分类页本就不存在，属设计如此）。
本次 44 页 → 45 个内部链接 → 只有 1 个真 404。
注意排除 `/cdn-cgi/l/email-protection`（Cloudflare 邮箱混淆，非真链接）。
308 = Cloudflare Pages 尾斜杠重定向，正常，浏览器会自动跟随。

### 站点现状（44 个 sitemap URL / 45 页）

- 63 项目、24 个分类、10 条链、500 条链上代币（10 链 × 50）
- SEO 元数据齐全（title/description/canonical/OG/Twitter 全有，仅 `/admin` 无，正常）
- `og-image.png` 存在且线上 200；robots.txt 带 sitemap；Cloudflare Web Analytics 已装
- `/api/auth` 存在返回 200（Decap CMS GitHub OAuth 正常）

### 待办（本次未做，已向用户报告）

**需要用户操作（非代码）**
- `src/data/affiliates.json`：8 个交易所 `code` 全空 → 联盟链接未激活（变现第一优先）
- `src/data/ad-network.json`：8 个广告位 `html` 全空 → 仍显示 "Your Ad Here" 占位
- GSC 重新提交 sitemap（现 44 URL）

**可由我完成（代码/数据）**
- 63 项目中 **48 个缺 `chains` 字段** → 卡片无链徽章；且 `getProjectsByChain()` 写好但
  **从未被调用**，链页只有 CoinGecko 代币、没有该链精选项目（SEO 价值损失）
- **全站 0 个 JSON-LD 结构化数据**（Organization/WebSite/BreadcrumbList/Article）
- 无 FAQ 页、无 Newsletter 订阅

## 用户确认后完成的三项（同日下午）

用户指令："ad-network.json 请用主流的行业内站点替代。chains字段请补充。FAQ和Newsletter订阅页面完成建立，订阅最好是用邮件订阅。"

### 1. 广告位改用主流行业站点（commit `febb5bf`）

不再等 Coinzilla/Bitmedia 的 tag，8 个广告位各自推广一个已在 projects.json 收录的
主流站点（Bybit / Trezor / OKX / CoinMarketCap / Magic Eden / DexScreener / Kraken / Coinbase）。

**关键设计**：推广链接走 `getReferralUrl(project)`，所以用户一旦填了 affiliates.json
的 code，8 个广告位自动变成联盟链接，无需再改代码。

优先级：直投广告(ads.json) > 广告网络 html tag > 主流站点推广 > Your Ad Here 占位。
保留 `html` 字段，将来接真广告网络直接填即可。

新增 `getNetworkPromo(slot)` + `NetworkPromo` 类型 + AdBanner 渲染分支。
全站 `__ placeholder__` 归零。脚本 `scripts/check-ad-slots.mjs` 可复查。

### 2. chains 字段补全（同 commit）

补 38 个（15 个原本就有）。**10 个有意留空**：新闻媒体（cointelegraph/theblock/
coindesk/tokeninsight）、审计（openzeppelin/certik）、安全工具（rekt）、税务（koinly）、
空投日历（airdropsio/coinmarketcal）——它们不部署在链上，加链徽章是错数据不是好数据。

链页接入 `getProjectsByChain()`（此前写好却从未调用），新增
"Best {chain} Projects & Tools" 区块，卡片带联盟链接，内容+变现双收。
分布：ethereum 47 / arbitrum 38 / polygon 38 / bsc 37 / base 33 / optimism 30 /
solana 27 / avalanche 19 / sui 7 / ton 6。链页 HTML 176KB→295KB（Brotli 后约 35KB）。

补数据脚本：`scripts/fill-project-chains.mjs`（含合法链 id 校验，非法即中断）。

### 3. FAQ + Newsletter（commit `e18ec19`）

- `/faq`：24 问 5 组，带 **FAQPage JSON-LD**——这是全站第一个结构化数据
- `/newsletter`：**真邮件订阅**，后端是新 Pages Function `POST /api/subscribe`
  - env：`NEWSLETTER_PROVIDER`(generic|mailchimp|buttondown) /
    `NEWSLETTER_ENDPOINT` / `NEWSLETTER_TOKEN`(Secret)
  - 自己不发邮件：Pages 无出站 SMTP，能向任意地址发信的端点等于开放中继
  - 未配置 → 503 `{unconfigured:true}`，前端自动显示 mailto 兜底，不会静默失败
  - 蜜罐字段防机器人；邮箱小写化+trim；上游 400 视为"已订阅"按成功处理
  - `SubscribeForm.astro` 组件复用，页脚也挂了，全站可订阅
- 测试：`test-subscribe.mjs` 33 条 / `check-faq-newsletter.mjs` 20 条
  / `check-live-links.mjs`（爬 sitemap 全站查死链，46 URL 零死链）
- `npm test` 现在串起：代理 28 + 订阅 33 + build + FAQ/Newsletter 20 + 广告位

### 页面数：44 → 46（sitemap）/ 45 → 48（build，含 404 与 admin）

### 仍然待办
- affiliates.json 的 8 个 code（**现在价值更高了**：广告位和链页卡片都走它）
- GSC 重交 sitemap（46 URL）

---

## 4. 全站 JSON-LD 结构化数据（commit `9f80416`）

### 架构：中央工厂 + 组件 + Layout 注入

- `src/utils/schema.ts`：所有 schema.org 节点的唯一来源
  （`organization` / `webSite` / `breadcrumbList` / `itemList` /
  `collectionPage` / `article` / `faqPage`，以及 `abs()` 相对路径转绝对）
- `src/components/JsonLd.astro`：多个节点自动折成一个 `@graph`，
  只输出一个 `<script>`、一个 `@context`；**转义 `< > &`** 防提交内容截断脚本标签
- `Layout.astro` 新增两个 prop：`breadcrumbs?: Crumb[]` 与 `schema?: unknown`
  → 传了面包屑就自动出 `BreadcrumbList`，全站继承

### 覆盖（线上已验证，17 条路由逐个 curl 确认 `@type`）

| 页面 | 节点 |
|---|---|
| `/` | Organization + WebSite + ItemList |
| `/category/*` | CollectionPage + ItemList + Breadcrumb |
| `/chain/*` | CollectionPage + ItemList(前30代币) + ItemList(项目) |
| `/learn/*` | Article + Breadcrumb |
| `/learn` `/compare` | CollectionPage + ItemList + Breadcrumb |
| `/compare/*` | Article + Breadcrumb |
| `/airdrops` | CollectionPage + ItemList（**只收 confirmed/ongoing**） |
| `/unlocks` | CollectionPage + ItemList（**只收 upcoming**） |
| `/faq` | FAQPage（改为走统一 helper）+ Breadcrumb |
| 静态页 | Breadcrumb（about/privacy/terms/submit/advertise/newsletter/dashboard） |

`dist` 统计：BreadcrumbList 45 页 / ItemList 25 / CollectionPage 24 /
Article 13 / FAQPage 1 / Organization 1 / WebSite 1

### 两个有意的取舍（别改回去）

1. **WebSite 不挂 `SearchAction`**：Header 搜索是客户端 Fuse.js，
   没有服务端渲染的 `/search?q=` URL。挂上去等于指向一个答不了查询的页面，
   属于结构化数据违规。等真有搜索结果页再补（schema.ts 里已写注释）。
2. **目录类 ItemList 的 url 指向外部站点**：本站**没有 `/project/*` 详情页**，
   写站内路径就是死链。`check-jsonld.mjs` 有一条断言专门防这个。

### 测试

- 新增 `scripts/check-jsonld.mjs`，**3268 条断言**，扫 `dist/**/index.html`：
  JSON 可解析 / 无 `</script` 越界 / Article 必填字段 / URL 必须绝对 /
  面包屑 position 连续且末项无 item / ItemList 非空且 numberOfItems 一致 /
  内部 URL 必须真实存在 / Organization 全站只出现一次
- 已接入 `npm test`（代理 28 + 订阅 33 + build + FAQ20 + 广告位 + JSON-LD）
- **坑**：`check-faq-newsletter.mjs` 原来直接读 `ld.mainEntity`，FAQ 改走
  `@graph` 后抛 TypeError 被 catch 吞掉——暴露出它那条
  `faq answers have text` 断言**从来没被执行过**。已改为在 `@graph` 里找
  FAQPage 节点，现在 20/20（之前是 19）。

### 数据改动
- `comparisons.json` 5 条各加 `publishedAt`/`updatedAt`（Article 需要真实日期）
  —— 注意：**别用 node 重写整个 JSON**，会把紧凑数组全部展开成 347 行噪音，
  用精确 Edit 插入，最终 diff 只有 10 行

### 线上状态
- 46 个 sitemap URL、46 条内链、**0 死链**
- 17 条代表性路由全部 200 且 `@type` 正确

---

## 5. 全站诊断 + 竞品分析 + 突围方案（产出 `CryptoNav-网站诊断与流量突围方案.md`）

### 实测发现（全部可复现，不是主观感受）

**内容结构**
- 46 页 / 38,038 词 / **中位 584 词** —— 低于 2026-06 核心更新后的「150 页主题深度」红线
  （该更新后 <150 页单主题站出现类目级崩塌，200-2000 页中型站掉 40-70%，权威站 +15-35%）
- **9 个分类页只有 279-392 词**（商业意图最强的钱页最薄），而 8 篇 learn 文章
  1405-1548 词（最没商业价值的内容最厚）——**内容投入与商业价值完全倒挂**
- **一手经验信号 0/46 页** —— YMYL 领域最致命。有一手经验信号 +38% 可见度，
  纯 listicle -63%

**技术**
- `cf-cache-status: DYNAMIC`，HTML **从不走 CDN 缓存**（连测 3 次）
  - 根因（Cloudflare 官方原文）：「一旦 Pages 项目加了 Functions，**默认所有请求都会调用
    Function**」。项目有 5 个 Functions 却没有 `_routes.json`
  - 已补 `public/_routes.json`（`include:["/api/*"]`）+ `public/_headers`，commit `5c5b60a`
  - **线上响应头仍未变化**，需在面板 Caching → Cache Rules 手动加一条
- 链页 **4 词/KB**（250-312KB HTML 承载 ~1100 词）；home 132KB
- header 14.9KB / **52 个链接**，占 learn 整页 32%（桌面+移动两套导航都写进 HTML）
- `auto.CcLMyVVA.js`（Chart.js）204KB 打进首页首屏

**SEO 基础面**
- **内链图完全平的**：46 个页面每一个都链向全部 9 个分类（header 硬编码），
  无 hub-and-spoke，权重无法集中；`/chain/ton` 内链数为 0
- 9 个 title >65 字符（SERP 截断）、6 个 <25 字符；5 个页面共用 Layout 默认 description
- robots.txt 是 **Cloudflare 托管版**，屏蔽了 GPTBot / ClaudeBot / Google-Extended /
  CCBot / Applebot-Extended / Bytespider（**战略取舍，待用户决策**）

### 竞品格局（关键洞察）

- **纯目录形态已被证伪**：DappRadar 2025-11 关停（7 年、2400 万 DAW、
  被机构引用，官方原话 "financially unsustainable"）。Cryptwerk 已腐化，
  唯一像样的直接对手是 CryptoLinks（单人运营，靠 **1,219 条骗局名单** 立身）
- **巨头不是靠写内容赢的**：DefiLlama / DexScreener 的自然流量几乎全是
  **品牌/导航词**，非品牌词接近零 —— 是**产品赢、SEO 收租**
- **唯一被验证的冷启动路径 = 可嵌入**：DefiLlama 头部外链来自 GitHub、
  Bitget 钱包、Morpho 官网、Tronscan；DexScreener 38K 引用域大量来自
  「每个代币页自动挂 View on DexScreener」。**可嵌入 = 外链飞轮**
- 巨头共同盲区：跨站对比、"best X for [场景]"、"Is X safe/rug/legit"、
  国别+监管、**任何需要编辑判断的内容**
- 行业正在集体失血：CoinGecko 43.5M→18.5M，CMC 157M→64M，Cointelegraph -99.9%，
  2025-09 前 77% 头部加密媒体失流

### 突围方案（一个主攻点，不铺七个方向）

**把「收录」升级为「核验」（Safety Verification）** —— 做第一个敢给判定结果的目录站。
8 个维度自动核验 63 个项目：域名年龄(WHOIS) / 合约是否可任意增发 / 蜜罐检测 /
审计情况 / 团队可验证性 / 历史安全事件 / 流动性深度 / 仿冒站检测。
产出 `/verify/[slug]` + Safety Score + 可嵌入徽章。

选它的四个理由（缺一不可）：
1. 巨头不敢做（定性判断有法律/商誉风险）→ 结构性缺口，不是暂时窗口
2. 真实搜索需求（"is X legit" 类高意图长尾）
3. 一次性解决两个致命伤：核验字段**就是专有数据**（被引用理由），
   核验过程**就是一手经验信号**（YMYL 的 E-E-A-T 门槛）
4. 可自动化，正好匹配已有的采集基建，边际成本极低

依赖顺序不能跳：核验数据 → 可嵌入徽章换外链 → 铺到 200+ 页 → 12-18 个月后谈流量

**立刻停止**：继续写通用百科内容。现有 8 篇 learn 文章在跟 CoinGecko Learn /
Kraken Learn / Coinbase Learn（DA 90+）抢词，必输，且占用了本该投入核验体系的产能。

### 时间预期（说真话）
- 6-12 个月起量不现实，**12-18 个月是基准**，且前提是 6 个月内走完上面那条链
- 核心更新受击后前 3 个月只改善 0-15%
- 含专有数据的内容被 AI 引用率 2 倍；**只改标题年份不改实质内容会被降权**
- 🔴 未找到任何可信的 2025-26 年"小团队 crypto 站 6-12 个月起量"公开案例
  （检索结果全是 SEO 代理商软文）

### AI 爬虫取舍（待用户决策）
- 被 AI Overview 引用的站点比未被引用的**多 35% 点击**，**93.8% 的 AIO 引用
  来自传统首页之外** → 对 DA=0 新站，AI 答案是少数还能「被引用而非排名」的渠道
- `Google-Extended` 管的是 Gemini 与模型训练，**不影响** Google 搜索的 AI Overviews 收录
- 建议：至少放开 PerplexityBot 和 GPTBot，保留 `ai-train=no`

## 突围执行（一）：核验管线落地 — 已推送 180c73b

用户指令「那按照你诊断，如何突围，请帮我突围」→ 执行诊断中的突围点：
把目录站改造成**可核验层**。

### 交付
- `scripts/build-safety-report.mjs` 核验管线（4 个免费无 key 数据源）
- `scripts/curation.mjs` 人工策展映射（含 REJECTED_MATCHES 显式记录拒配）
- `scripts/check-safety.mjs` 1298 条断言（已接入 `npm test`）
- `src/data/safety.json` 63 个项目真实核验数据
- `/verify`（索引）+ `/verify/[slug]`（63 页）+ `/embed/[slug]`（63 个 2.3KB 徽章）
- `SafetyBadge` 组件接入 ProjectCard / 首页 / Header / Footer
- `.github/workflows/refresh-safety.yml` 每周一 07:00 UTC 刷新
- 页面总数 45 → 174

### 数据源实测结论（反复踩坑得出，别再试错）
1. **RDAP**：IANA 引导文件**不含 .io / .co**。.io 走 `rdap.identitydigital.services/rdap/`
   可通；.co 全端点不可达（theblock.co 因此永久无域名年龄）。Verisign 会限流，需重试。
2. **GoPlus 免费档一次请求只返回 1 个地址**，传 15 个也只回第一个 → 批量请求会**静默丢弃**。
   必须逐地址请求。
3. **DefiLlama /hacks 的 `defillamaId` 在 CEX/钱包类事件上大量缺失**（Binance $570M、
   Kraken $10.4M、Bitget $100M 全无 ID），且协议按版本拆 ID（Aave V1–V4 是 4 个）。
   只能人工策展归属，但**金额/日期全部实时取自源数据**。
4. **Wayback CDX 在沙箱代理下不可达**（web.archive.org 被白名单拦截），CI 里应可用。
5. 沙箱代理只放行部分 host：binance.com / aave.com / uniswap.org 均 `fetch failed`，
   这是**环境限制不是站点问题**，探针要用 SKIP 而非 FAIL 区分。

### 三个致命陷阱（都是"数据看起来对但实际会毁掉产品"）
1. **域名注册时间 ≠ 项目年龄**。solana.com=1993、base.org=1996、bybit.com=1999、
   ledger.com=1994、dune.com=1997、aave.com=1997、sushi.com=1999 — 全是二级市场买的
   溢价老域名。直接拿来算年龄 = 公开写「Solana 运营 33 年」。
   → 对策：以 Wayback 首次抓取为准；无 Wayback 且注册日早于 2008-10-31（比特币白皮书）
   时，`operatingSince` 置 null（不发布运营年龄）+ 分数打 0.4 折 + 页面显示警告。
   **域名注册日期仍作为事实展示，但标注它不是项目年龄。**
2. **DefiLlama 文本匹配会诬告**。"Blur Finance"（BSC 跑路盘）≠ Blur.io、
   "Phantom Galaxies"（NFT 游戏）≠ Phantom 钱包、"Safe Dollar" ≠ Safe{Gnosis}、
   "Solido Cash" ≠ Lido、"RocketSwap/Grand Base" ≠ Coinbase 的 Base、
   "AVE" 撞 Aave/Agave/xAVE/Save。→ 人工策展 + REJECTED_MATCHES 作为数据发布。
3. **低覆盖度反而拿满分**。MEXC 只有 1 个维度有数据，却因「无事件」得 100 分。
   → 二维度门槛（<2 个维度不评分）+ **分数封顶于覆盖度**（只验了 75% 的评分项，
   最多只能得 75 分）。封顶前 17 个项目并列 100 分，封顶后分布 58–98。
   「我们明明写着这不是审计，却给满分」是最不能犯的错。

### 评分模型
- longevity 35% / incidents 40% / contract 25%
- 缺失维度**权重按比例重分配**（无代币的 CEX 不因没有代币被扣分）
- 维度分先归一化到 0–100 再加权（曾漏掉这步，导致满分只有 32.7）
- 事故惩罚 = 严重度(按金额分档) × 时间衰减(1年内1.0 / 1-3年0.8 / 3-5年0.6 / >5年0.4)
- 结果：A 11 个（全是 100% 覆盖的代币项目）/ B 44 / C 7 / 未评级 1（The Block）
- 垫底：Bybit 58（史上最大 $14 亿黑客）、Bitget 58、Kraken 63、OKX 66 — 符合事实

### 管线性能教训
- 初版 9 分钟跑不完：对网络错误做 3 次重试 × 25s 超时 × 63 域名。
  → **网络层错误（含 TimeoutError）不重试** + 熔断器（连续 3 次失败即跳过该源）→ 22 秒。
- 无状态重跑会**丢已有数据**（上一轮成功的 pancakeswap/arbitrum 下一轮失败）。
  → 按不可变性缓存：RDAP 永久缓存，GoPlus 7 天，Wayback 30 天，hacks 每次拉新。

### 待办（下一步）
- CI 首次跑通后确认 Wayback 生效（本地无法验证），届时 15 个「老域名」项目
  会从 `low` 置信度升为 `high`，分数会上升
- arkham 的 GoPlus 始终 no-result，合约地址待复核
- 徽章外链飞轮需主动 BD：联系项目方嵌入 /embed/[slug]
- 用户仍有旧待办：affiliates.json 8 个 code 字段、Cloudflare 缓存规则（_headers 未生效）、
  Newsletter 环境变量、AI 爬虫放开决策

---

## SEO 元数据门禁（同日续，commit f46bede）

### 起因
审计里手查出的 67 个 title/description 问题。不打算「修一次」，而是加门禁让它不能回来。

### 新增资产
- `scripts/check-meta.mjs` — 扫 `dist/`（不是源码）的 title / description 门禁
- `src/utils/seo.ts` — `fitTitle(candidates, max)` / `fitDescription(text, max)`
- `scripts/apply-meta-edits.mjs` — 一次性迁移脚本（外科式字符串替换）
- `package.json` 加 `test:meta`，串进 `npm test` 链

### 为什么扫 dist 而不是源码
源码层面看不出两类失败：
1. 页面忘了把 description 传进 Layout（6 个页面共用同一句默认描述）
2. title 只有在 HTML 实体解码后才知道真实长度

### 三个自己踩的坑（都是先错才发现的）
1. **正则 `[^"']*` 会在 `cryptocurrency's` 的撇号处截断** → 197 字符的描述被量成 36 字符，
   报了一堆假的「太短」。修法：捕获分隔符再反向引用 `content=(["'])([\s\S]*?)\1`。
   **教训：写完测量工具先拿已知长度的样本验证，别直接信它报的数字。**
2. **手数字符数错了三次**（/verify 描述我数成 152，实际 188）。
   → 改成让 `fitDescription()` 在代码里保证，不靠我算术。
3. **JSON.parse + stringify 回填会把手写的内联对象全部重排**，
   30 行的内容变更变成 865 行 diff，改动淹没在噪音里。
   → `git checkout --` 回退，改用逐条精确替换（34 行 diff）。

### 具体修复
- /verify 标题改为**多候选择优**：`fitTitle` 挑最长的、仍 ≤60 字符的变体。
  硬编码一个后缀等于赌数据永远不会变长（"Safe (Gnosis Safe)" 18 字符 vs "Blur" 4 字符）。
- /learn 去掉 ` | CryptoNav` 后缀：内容页上这 12 个字符当关键词比当品牌值钱。
  8 篇文章 title 相应缩短（84→52 等），描述 165-206 字符砍到 139-159。
- /category 新增 `seoTitle` + `seoDescription`（"NFT - CryptoNav" 只有 15 字符，排不了任何词）。
  描述里的数量用 `{n}` 占位、渲染时替换真实数量 —— 手写「12 platforms」第一版就错
  （该分类实际 14 个，且 `category.split('-')[0]` 的计数方式与 `getProjectsByParentCategory` 不同）。
- /about / /faq / /privacy / /terms / /submit / /404 / /advertise / /newsletter 各自补描述，
  消掉 6 页共用一句默认描述的问题。

### 结果
67 失败 → 0。334 条断言通过。
全量测试：cg 28/28、subscribe PASS、safety 1298、build 174 页、build-check 20/20、
ads 0 占位、jsonld 4914 通过 0 失败、meta 334 通过。
已推送 origin/main（`180c73b..f46bede`）。

---

## 评分模型修正：覆盖率硬上限 → 先验收缩（commit 5213991）

### 触发
做分布图时发现 **62 个已评级项目里有 33 个都是 75 分**。

### 根因
`total = Math.min(raw, coverage)`。63 个项目里 45 个是 75% 覆盖率（域名+事故，无代币），
上限把其中 33 个全部钉死在 75。

**教训：又是一次「看输出才发现」的 bug。** 之前三次（维度未归一化、单维度满分、
把我们的基础设施故障算进扣分）也都是跑了才暴露。评分模型必须看分布，不能只推逻辑。

### 改法
```
total = raw × coverage + PRIOR × (1 - coverage)
```
保留原意图（部分数据拿不到满分），恢复区分度。PRIOR 取值**跑数据标定**出来的：
- 55 → A 26 / B 21 / C 15（A 太多，三项自动检查撑不起 42% 的 A）
- **50 → A 19 / B 36 / C 7** ← 采用
- 45 → 同 50；40 → 开始出现 D

### 顺带修的第二个问题
域名注册早于比特币的旧规则是 `discount = 0.4`，等于**把我们的数据缺口算成项目风险**。
结果：Solana / DeBank / Dune / CoinDesk（零事故）和 Binance（$6.1 亿）同分 67。
→ 改 `discount = 0.5`（中性：无证据时假定平均）。仍有区分：可验证年龄的项目仍排在前面。
这与之前修的「Binance 因 Wayback 挂掉被扣分」是同一类错误，之前只修了一半。

### 结果
33 个同分 → **20 个不同分值，区间 60–98**，A 19 / B 36 / C 7 / 未评级 1。
垫底：Bybit 60（$14 亿）、Bitget 60、Kraken 64、OKX 66、Ledger 66 — 全是有真实事故的，排序正确。

### 新增护栏（check-safety.mjs，1298 → 1345 断言）
- 覆盖率 <100% 时分数必须 <100（部分数据不能满分）
- 任一单个分值不得超过目录的 1/3（防同分墙再现）
- 不同分值的数量 ≥ 项目数的 1/3

### 注意
`operatingSince` / `ageYears` 在数据里但**页面从未渲染**——这是对的。
页面只说 "domain registered on X" 或 "first captured online X"，从不声称项目运营了多久。
Blur 域名 2013 年注册但项目 2022 年才上线，这个 9 年误差只能靠 Wayback 在 CI 里纠正，
不能手工打补丁（那又回到手写断言的老路）。

## 部署故障根因定位与修复（本轮，已推送 9885cd4）

### 报错
```
Failed: an internal error occurred. If this continues, contact support
Error: Failed to publish assets.
```
**关键点：构建成功、只有发布资源阶段失败。** 所以本地 `npm run build` 永远全绿，
排查方向必须落在「产物本身」而不是「构建过程」。

### 根因：public/logos/dex 无上限累积
`scripts/fetch_dex_wins.py` 每天下载 DexScreener 涨幅榜代币的 header 图：
- 只判断了下限 `len(img) > 200`，**没有上限**
- 一律存成 `.png`，但 DexScreener 的 header 很多是**动画 GIF**（魔数 GIF89a）
  —— 单个「代币图标」最大 7.46 MB
- **从不删除旧文件**，每天加几个，攒到 106 个 / 45 MB，而 wins.json 只引用 7 个
  → 99 个孤儿

dist 因此涨到 **67 MB / 695 文件**。清理后 **21 MB / 596 文件**。

### 排查方法（可复用）
1. `du -sh dist` + `find dist -type f | wc -l` 看总量
2. `find dist -type f -printf '%s\t%p\n' | sort -rn | head` 找大文件
3. **用 PNG 头解析宽高验证文件是否真的是 PNG**：解析出 `33x4278931013` 这种
   荒谬值 → 说明根本不是 PNG。`head -c 6 file` 看魔数即可（GIF89a / \xff\xd8\xff / \x89PNG）
4. 对比「目录里的文件数」vs「数据文件里引用的数量」→ 差值就是孤儿

### 教训
- **只设下限不设上限的检查等于没检查。** `len(img) > 200` 挡住了错误响应，
  放过了所有超大文件。
- **自动生成的文件必须有对应的清理机制**，否则 dist 会静默膨胀到部署失败。
- **本地 build 绿不代表能部署。** 需要 `check-dist-size` 这类门禁盯产物体积
  （阈值：总 40 MB / 单文件 20 MB / 5000 文件），比 Cloudflare 硬限制
  （单文件 25 MB / 20000 文件）更紧，好在有征兆时就发现。

### 新增文件
- `scripts/prune-dex-logos.py` —— 删孤儿 + 删超限 + 同步清空 wins.json 引用
  （UI 有首字母兜底，不会出坏图）。幂等，支持 `--dry`。
- `scripts/check-dist-size.mjs` —— 产物体积门禁，已接入 `npm test`
- `scripts/check-robots.mjs` —— robots.txt 门禁（48 条断言），已接入 `npm test`
- `scripts/strip-fake-referrals.mjs` —— 一次性清理脚本（假联盟码）

### 定时任务的坑
推送时远端已有新提交 `5d5bae7`（定时任务 10:00 跑的），它用的是**修复前的旧脚本**，
把 101 个 logo 全下回来了。合并后必须**重新跑一次 prune** 才干净。
→ 合并远端改动后，凡是涉及自动生成资源的，都要重新跑清理。

### 本轮同时完成的其他两件事
1. 删掉 8 条伪造 referral（`ref=Cryptonav`），链接回退到 website
2. AI 爬虫策略 A：允许引用 + 拒绝训练（详见下一段）

### AI 爬虫策略（用户选了「选项 A：允许引用 + 拒绝训练」）
已写入 `public/robots.txt`，并建了 `scripts/check-robots.mjs` 门禁（48 断言）。

**实现要点：拒绝训练靠 `Disallow`，不靠 `Content-Signal`。**
查了官方文档后才确认正确做法（我原本在手册里写的方案是错的）：

| 爬虫 | 用途 | 处理 |
|------|------|------|
| OAI-SearchBot | ChatGPT 搜索结果 | Allow |
| Claude-SearchBot | Anthropic 搜索索引 | Allow |
| PerplexityBot / Amazonbot | 引用型 | Allow |
| ChatGPT-User / Claude-User / Perplexity-User | 用户发起的取回 | Allow |
| **GPTBot** | OpenAI 训练 | **Disallow** |
| **ClaudeBot** | Anthropic 训练 | **Disallow** |
| **Google-Extended** | Gemini/Vertex 训练 | **Disallow**（不影响 Googlebot 排名） |
| **Applebot-Extended** | Apple AI 训练 | **Disallow**（不影响 Siri/Spotlight） |
| meta-externalagent/fetcher、Bytespider、CCBot | 训练 | Disallow |

OpenAI 官方文档明确说：「can allow OAI-SearchBot in order to appear in search
results while disallowing GPTBot to indicate that crawled content should not be
used for training」—— 这是官方支持的组合，不是权宜之计。
Anthropic 官方文档同样区分了 ClaudeBot(训练) / Claude-SearchBot(搜索) / Claude-User(用户发起)。

`Content-Signal: ai-train=no, search=yes, ai-input=yes` 仍然输出在通配符组上，
作为面向未来的声明（Cloudflare Content Signals 机制，语法来自 contentsignals.org）。
但该机制自己也声明「部分爬虫可能忽略」——**真正生效的是 Disallow**。

### 抓取官方资料的一个方法
交易所联盟计划页面（binance/okx/bybit）全部反爬，WebFetch 拿不到。
但 **OpenAI / Anthropic 的开发者文档可以正常抓取**。查这类规范性问题优先找官方文档，
比搜二手博客可靠得多（搜索结果全是 SEO 垃圾站）。

---

## Cloudflare Pages 部署失败：`Failed to publish assets`（第二轮，推翻第一轮结论）

### 第一轮结论是错的
我之前判定「产物体积过大」是根因，把 `dist` 从 67 MB 清到 21 MB。**实测证伪**：

用 `git worktree` 检出最后一次成功部署的 `2e624fa` 单独构建：

| 版本 | 文件数 | 体积 | 结果 |
|------|--------|------|------|
| `2e624fa`（最后成功上线） | 568 | **61.53 MB** | ✅ 成功 |
| 当前 HEAD | 630 | **21.89 MB** | ❌ 失败 |

**61 MB 成功、21 MB 失败** → 体积不是原因。
（清理 dex logo 仍是对的——那是真 bug，只是不是这次的解药。）

### 逐项排除（这套方法可复用）
| 假设 | 结论 | 判据 |
|------|------|------|
| 总体积 / 单文件 / 文件数 | 排除 | 上表；最大文件 5.06MB < 25MB；630 < 20000 |
| `_routes.json` 格式 | 排除 | 官方文档三约束全满足（≥1 include、≤100 规则、≤100 字符） |
| `_headers` 格式 | 排除 | 已在线上生效，见下 |
| 非法 UTF-8 / NUL / 空文件 / 非 ASCII 路径 / 大小写冲突 | 排除 | `scripts/scan-dist-integrity.mjs` |
| `build` 脚本被改 | 排除 | 一直是 `astro build` |
| Actions 抢跑 | 排除 | 5 个 workflow 全 schedule+dispatch，无 push |

### 三个关键诊断手法（值得复用）

**1. 用「静态 vs Functions 响应头」判断 `_headers` 是否生效**
官方文档：_headers 只作用于静态资源，不作用于 Pages Functions 响应。于是：
- 静态资源 `/admin/config.yml` → 有 `x-content-type-options: nosniff` + `referrer-policy`
- Functions `/api/cg/global` → 两个头都没有

「静态有、Functions 没有」就是 `_headers` 生效的签名。由此推出 `5c5b60a`
（引入 `_headers`/`_routes.json` 的提交）**部署成功过**，第一次失败是 `180c73b`。

**2. 不要用 `Cache-Control` 判断 `_headers`**
Cloudflare 的 Browser Cache TTL（默认 4h = `max-age=14400`）会**覆盖** `_headers`
里的 Cache-Control。设了 `max-age=31536000, immutable`，线上仍是 14400 → 误判。

**3. 不要用 U+FFFD 找非法 UTF-8**
压缩后的 JS 里本来就有字面 U+FFFD（`t.REPLACEMENT_CHARACTER="\uFFFD"`），
那是合法码点。必须用 `new TextDecoder('utf-8',{fatal:true}).decode(buf)` 才算数。

**4. 308 不是「页面不存在」**
Pages 对目录页的 308 是补尾斜杠重定向，`/faq` → `/faq/`。要用 `-L` 跟随后再判断，
否则会把一堆正常页面误判成「未上线」。

### 真正的结论：Cloudflare 平台侧故障
决定性证据：**`3ed8bc6` 是纯文档提交**（只改 memory 文件），产出的 `dist` 与上一个提交
逐字节相同 —— 它也失败。内容导致的失败无法解释这一点。**失败与时间相关、与内容无关。**

佐证：1.5 秒就失败；报错是笼统的 "internal error" 而非 Cloudflare 对校验失败
会给的明确信息（如 "File X is larger than 25 MB"）。
同类报告：cloudflare/workers-sdk#13259（2026-04，发布流水线故障，与用户代码无关）。

### 解法：绕开 Pages 内置 CI
新增 `.github/workflows/deploy-pages.yml` —— 用 `wrangler@4 pages deploy` 从 Actions 发布。
- 绕过 Pages Git 构建器
- Wrangler 给真实报错
- 内置 3 次重试（间隔 25s）吸收瞬时故障
- `workflow_dispatch` 手动触发（CI 还开着时避免双重构建）
- 需要 secrets：`CLOUDFLARE_ACCOUNT_ID` + `CLOUDFLARE_API_TOKEN`

### 新增资产
- `scripts/scan-dist-integrity.mjs` — 产物完整性扫描（严格 UTF-8 解码 / NUL / 空文件 /
  非 ASCII 路径 / 长度 / 大小写冲突），容忍 `.gitkeep`，已接入 `npm test` 作 `test:dist-integrity`
- 手册 `CryptoNav-待办操作手册.md` 已补「三条解封路径」+ 可直接提交的工单正文

### 教训
1. **「看起来像原因」不等于「是原因」**。45 MB 垃圾是真的、清理是对的，
   但我在没验证的情况下就宣布它是根因 —— 应该先做对照实验再下结论。
2. **对照实验要选对基线**：把「最后一次成功的提交」单独检出构建，
   测出它 61 MB 也能上线，一句话就推翻了整个假说。这比继续猜快得多。
3. 本地 `npm test` 若 `dist` 已存在，Astro 的 `cleanServerOutput` 会撞上沙箱删除守卫
   导致构建非 0 退出、测试链中断。**先 `rm -rf dist` 再跑即可**（CI 上无此问题）。

### 后续：`deploy-pages.yml` 加了项目名预检
`wrangler pages deploy --project-name=X` 在 X 不存在时**不报错，而是新建项目**。
新项目无自定义域名 → 部署显示成功但 cryptonav.site 依旧跑旧版（把故障伪装成修好）。
因此在发布前先调 `GET /accounts/{id}/pages/projects` 校验名字。

刻意做成非对称：
- 名字**确定不存在** → 失败退出
- API 不可达 / success=false / 响应非 JSON → 只警告并继续

这样"token 没有读权限"或"Cloudflare API 抖动"不会误杀一次本可成功的部署。
五个分支都本地验证过（命中/未命中/API失败/空列表/非JSON）。

---

## Wrangler 部署首跑失败（f824985）：3 次尝试全败

### 现象
用户跑 `Deploy to Cloudflare Pages (Wrangler)`，Annotations 只有：
`2 errors and 4 warnings` / `Process completed with exit code 1` /
`all 3 publish attempts failed` / `attempt 1/2/3 failed` + 一条 Node 20 弃用警告。
**真正的 wrangler 报错被折叠在 Publish 步骤的 `::group::` 里，Annotations 看不到。**

### 从 Annotations 本身能推出的两件事（重要推理技巧）
1. 只有 2 个 error 且都来自发布步骤 → **没有** "no Pages project named ..." 报错
   → 项目名是对的，且 token 能正常列出项目列表（= token 至少有 Read 权限）。
2. **3 次重试全部失败 = 确定性失败**，不是偶发抖动。抖动至少会侥幸成功一次。
   → 原因在配置或平台，不在运气。

### 最可能的凶手：只读 token
页面列表检查只需要 Read，**部署需要 Edit**。只读 token 能通过预检但部署必败，
表现和平台故障一模一样（三次全败 + 报错含糊）。这是本次排在第一位的假设。

### 改动（commit 866a7da，已推送）
- 新增 `scripts/verify-cloudflare-token.mjs`：调 `GET /user/tokens/verify`
  - 硬失败：token 被拒 / 非 active / 已过期 / **完全没有 Pages 权限**
  - 警告：Pages 权限不含 Write|Edit（可能只读）；账号 scope 与 `CLOUDFLARE_ACCOUNT_ID` 不符
  - 过期前 14 天预警
  - **为什么抽成脚本文件而非内联 `node -e`**：分支逻辑必须可测，而 YAML 里的内联
    node 脚本无法测、还容易被 shell 展开搞坏（此前踩过 "Bad substitution"）。
- `deploy-pages.yml`：
  - 第 3 次尝试开 `WRANGLER_LOG=debug`（打印每次 API 调用的 HTTP 状态与响应体，
    这是区分「认证问题」与「平台故障」的决定性证据）
  - 失败时先完整打印 attempt 1 的简洁报错，再打印 attempt 3 debug 日志的尾部 150 行
    （完整日志作为 `wrangler-deploy-log` 制品上传）
  - 附「错误形态 → 该改哪个 secret」对照表
- 手册补 2.5（f824985 复盘 + 三种原因按可能性排序）和 2.6（重跑与读日志指引）

### 测试手法（可复用）
用 `node --import ./.tmp-fetch-mock.mjs ./scripts/xxx.mjs` 在预加载里覆盖
`globalThis.fetch`，即可**不改生产代码**地跑通 11 个分支（good/rejected/disabled/
expired/expiring/nopages/readonly/unsuffixed/wrongaccount/unrestricted/非JSON）。
比在 YAML 里塞 mock 干净得多。

### 通用教训
4. **先问"重试全败意味着什么"，再去找原因。** 失败形态本身携带信息：
   全败 = 确定性，指向配置/平台；零星失败 = 抖动，指向网络。
5. **权限类预检要分清 Read 和 Write。** 用"能列出"去证明"能部署"是无效证明 ——
   列项目是 Read，部署是 Edit。这是很容易犯的证明缺口。
6. **GitHub Actions 的 `::group::` 会吞掉你最需要的那行报错。**
   关键失败输出要么 `tee` 到文件后在 group 外重打，要么上传制品。

---

## 第二轮：我又引入了一个 bug（checkout 顺序），以及怎么从弃用警告读出来

### 现象
改完上一轮后重跑，Annotations 变成 `1 error and 1 warning`：
- error: `Process completed with exit code 1.`（**没有任何错误文字**）
- warning: Node 20 弃用，只列了 `actions/upload-artifact@v4`

### 决定性线索：那条"噪音"警告其实是最有用的证据
上一轮的警告列的是 `actions/checkout@v4, actions/setup-node@v4`；
这一轮**只剩 `actions/upload-artifact@v4`**。
→ checkout 和 setup-node **根本没执行**，任务在它们之前就挂了。
→ 而 upload-artifact 会执行，是因为它挂着 `if: failure()` —— 反证确实有步骤失败了。

**结论：别把 Node 20 弃用警告当噪音划掉。它列出了本次实际加载过的 action，
等于一份"执行到哪一步"的免费探针。**

### 根因（我的 bug）
上一轮我把内联的 `node -e` 抽成了 `scripts/verify-cloudflare-token.mjs`，
**但没注意这个校验步骤排在 `actions/checkout@v4` 之前**。
runner 上还没检出代码，文件不存在 → `node` 报 `can't open file` 到 stderr，
**GitHub 不会把 stderr 变成 annotation** → 只剩一句光秃秃的
`Process completed with exit code 1.`

内联 `node -e` 时不需要文件所以没暴露，抽成脚本文件就炸了。
**重构时改动了"步骤依赖什么"，就必须重新检查"步骤排在哪里"。**

### 修复（commit 284ca3a，已推送）
1. `checkout` + `setup-node` 提到第 2、3 步，排在所有校验之前。
   仍排在 `npm ci` 之前 —— token 有问题依然几秒内失败，不用等完整安装。
2. 给 token 校验加了文件存在性守卫：
   ```bash
   if [ ! -f scripts/verify-cloudflare-token.mjs ]; then
     echo "::error::scripts/verify-cloudflare-token.mjs not found -- checkout did not produce it"
     exit 1
   fi
   ```
   **一个专门用来做诊断的预检步骤，自己绝不能静默失败** —— 这次的代价是一整轮 CI。
3. 裸的 `npm ci` 步骤改名 `Install dependencies`。

步骤顺序现为：secrets → checkout → setup-node → token 校验 → 项目名校验
→ Install dependencies → Build → 完整性 → 装 wrangler → 发布(3次) → 上传日志

### 通用教训（第 7、8 条）
7. **Annotation 里"少了什么"和"多了什么"同样重要。**
   warnings 的**差异**（尤其跨运行对比）能定位执行断点。
8. **改了实现形态，要回头检查它在流程里的位置。**
   内联 → 外部文件、本地值 → 远程调用，都会悄悄引入新的顺序/环境依赖。

---

## 第三轮：锁定根因 = token 只读（有硬证据），并修掉我自己造的误报

### 自动检查报了错，但那是误报
workflow 报 `the token has no Cloudflare Pages permission at all -- it can never deploy`。
**这是我的检查脚本误判。** 它用 `/pages/i` 匹配权限组名字来判断，
而 Cloudflare 不保证权限组命名稳定 —— 而且我把这种"提示"当成了**硬失败**。

### 推翻误报的硬证据
查 `git show f824985:.github/workflows/deploy-pages.yml`，确认那次运行**包含**项目名校验，
且它**通过了**（该步骤非 200 时会发 warning，而 f824985 的 4 条 warning 里没有它）。
→ `GET /accounts/{id}/pages/projects` 返回了 200。
→ **列项目需要 Pages 读权限** → token 一定有 Pages 权限。
→ 所以"完全没有 Pages 权限"必然是假的。

**结论：token 有 Read、没有 Edit（只读）。** 这正是第二轮排在第 1 的假设。

### 修复（commit e07c2ae，已推送）
1. `verify-cloudflare-token.mjs`：Pages 权限判定**降级为警告**。
   只有「被拒绝 / 非 active / 已过期」才是硬失败。
   名字匹配不可靠 —— 它是提示，不能据此拦截一次本可成功的部署。
2. `deploy-pages.yml`：**让项目名校验承担权限判定**（它真调 Pages API）。
   - 401/403 → 硬失败，并写明 **Pages 是 Account 级权限，Zone 级 token 会被拒**（高频坑）
   - 200 → 打印 "Pages API accepted this token -- it has at least read access"
3. 手册：把"按可能性排序的猜测表"换成**确定的结论**，新增 2.6（重建 token 的精确步骤：
   Read vs Edit、Account vs Zone 两个高频错误），原读日志指引顺延为 2.7。

12 个 mock 分支全部通过，含一个★回归用例（token 实际可用但权限组名不含 "Pages"
→ 现在只警告，不再拦截）。

### 给用户的动作
重建 token：Account → Cloudflare Pages → **Edit**（不是 Read），
Account 级（不是 Zone 级），更新 `CLOUDFLARE_API_TOKEN`。
`CLOUDFLARE_ACCOUNT_ID` 不用动（已证明是对的）。

### 通用教训（第 9、10 条）
9. **自动检查报的错，也要先验证再采信。** 这次靠"上一轮的运行证明 token 能列项目"
   推翻了自己的检查。诊断工具本身有 bug 时，它会用一种很像真相的语气骗你。
10. **用"名字/元数据"判定权限，只能当提示；用"实际调用"判定，才能当结论。**
    名字不稳定（API 版本差异），调用结果确定。两者冲突时永远信调用。

---

## 事故：本地 .git 对象丢失（本沙箱环境会删 .git 下的文件）

### 现象
`git push` 被拒（远程有新提交 `bc8ae27`，是 airdrops 定时刷新）。
按约定 `git merge FETCH_HEAD` → 被 SIGTERM 中断 → 之后：
```
error: Could not read 284ca3aa78ccd99e1393d236b65e9218e22b1a4a
fatal: Failed to traverse parents of commit e07c2ae
error: bad tree object HEAD
```

### 根因
本环境有个进程**持续删除 `.git/` 下的文件**，两次观察到：
1. 之前：`.git/refs/remotes/origin/` 建好即被删（`git status -sb` 一直显示 `[gone]`）
2. 这次：`.git/objects/28/4ca3aa...`（本地提交 284ca3a 的 loose object）被删

排查过的干扰项：`.git/orphan-pack-backup/` 里只有旧 idx，无备份；
pack 文件日期是 8/29、8/30，说明不是 git gc 打包后清理的。

### 恢复步骤（可复用）
1. **先备份工作区里不可替代的文件**（未提交的记忆文件、未推送的改动）
2. `git fetch --refetch origin` —— 强制重下全部对象，修复缺失（本次 1m18s）
3. `git reset --hard <远程 tip>` —— 丢弃本地坏提交
   （未推送提交的 tree 也丢了，但内容在工作区/备份里，重建即可）
4. 从备份恢复文件 → `git diff` 核对差异符合预期 → 重新 commit + push

**关键：远程一直完好**（所有推送过的提交都在）。损失仅限"未推送的部分"，
而那部分在备份里。push 被拒时先确认远程状态，别慌。

最终：`bc8ae27` → `63bfe58`，工作区干净，文件行数核对无误
（workflow 278 行、记忆 706 行）。

### 教训
11. **本环境下 `.git` 会被外部清理，本地提交随时可能丢对象。**
    重要改动**尽快 push**，别把未推送的提交当长期存储。
12. **push 被拒时先看远程多了什么**（`git log --oneline HEAD..FETCH_HEAD`）。
    本次远程多的是自动化数据刷新提交（airdrops，12 小时一次）。

---

# ★★★ 真正的根因：`_routes.json` 缺 `exclude` 字段 ★★★

## 结论
`public/_routes.json` 原来是：
```json
{ "version": 1, "include": ["/api/*"] }
```
**缺 `exclude` 字段 → Wrangler 判定为非法 → 每次发布都被拒。**

Wrangler 4.127.1 的源码（`wrangler-dist/cli.js`，`isRoutesJSONSpec`）：
```js
return typeof data === "object" && data && "version" in data
  && typeof data.version === "number" && data.version === ROUTES_SPEC_VERSION
  && Array.isArray(data.include)
  && Array.isArray(data.exclude)     // ← 必须存在，undefined 直接判非法
  || false;
```
报错走 `INVALID_JSON_SPEC` 分支，文案正是日志里那句
`Please make sure the JSON object has the following format:`。

**Cloudflare 文档说 `exclude` 可选，实现却要求必须存在。文档和实现不一致。**

## 我之前错在哪（重要）
我在排查早期就"排除"过 `_routes.json` —— 但我是拿**文档**的三条约束
（至少一条 include / 规则总数 ≤100 / 单条 ≤100 字符）去核对的，它全过，我就宣布无辜。
**核对错了对象：应该对着实现（Wrangler 源码）校，不是对着文档。**

由此还推出了一个错误的结论："docs-only 提交产生相同 dist 也失败 → 与时间相关、
与内容无关 → 平台侧故障"。**这个推理是错的**：dist 里始终带着那个非法文件，
所以"内容没变"恰恰意味着"每次都必败"。现象一致，成因完全相反。

同理，"体积 61 MB 能上线、21 MB 失败"也一直是无关的干扰项。

## 时间线复盘
1. 某次提交引入/修改 `_routes.json`，去掉了 `exclude`
2. 此后**每次**部署在发布阶段被拒
3. Pages 内置 CI 只报 `Failed to publish assets`（不提 routes）→ 误导成平台故障
4. 我据此建了 Wrangler 部署通道 → 它报出 `Invalid _routes.json` → 真相大白

## 修复
1. `public/_routes.json` 补上 `"exclude": []`
2. 新增 `scripts/check-routes-json.mjs`：**照抄 Wrangler 源码规则**做校验
   （含 `exclude` 必需、include 非空、总数 ≤100、单条 ≤100 字符、必须以 `/` 开头、
   同一列表内 splat 规则不得重叠）。7 个分支本地验过 + 实测抓出旧的 dist 文件。
3. `package.json` 加 `test:routes-json`，接进 `test` 链（在 build 之后，
   同时校验 `public/` 和 `dist/`）
4. `deploy-pages.yml` 发布前硬校验（无效必然发布失败，不能只警告）

**关键设计：校验脚本的注释里写明了"规则抄自 Wrangler 4.127.1 源码，不是文档"，
以及文档与实现的具体分歧点。** 否则下次有人照文档"简化"这个文件，故障会原样复现。

## 教训
13. **验证合规性要对着实现，不是对着文档。** 文档会过时、会与实现不一致。
    有源码/有实际校验器时，以它为准。这次为此付出了一整天。
14. **"现象一致"不等于"成因一致"。** 我用"docs-only 提交也失败"推出"与内容无关"，
    但那个推理默认了"内容是无辜的"——循环论证。应该反过来问：
    "有没有某个**一直存在**的内容会导致必败？"
15. **当一个组件有多个实现（Pages CI / Wrangler）时，报错更具体的那个才是可信来源。**
    Pages CI 报 "Failed to publish assets"，Wrangler 报 "Invalid _routes.json"。
    早一点用 Wrangler 跑一次，能省下大半天的猜测。

---

# 部署打通后的收尾（21:00-21:20）

## 用户反馈「成功了，但是有3个警告」

线上验证（全部通过）：
- 首页 title 已是新构建的 `CryptoNav — Crypto Directory: Exchanges, Wallets &amp; DeFi`（破折号 + `&`，旧版是连字符 + `|`）→ 确认新包已上线
- `/verify/` 200（此前 404）；`/verify/` 页面含 2 处 "Verified"
- `/compare/ /learn/ /airdrops/ /unlocks/ /dashboard/ /chain/ethereum/ /chain/solana/ /advertise/ /submit/ /verify/` 全部 200
- **`/api/cg/simple/price?ids=bitcoin&vs_currencies=usd` → `{"bitcoin":{"usd":77867}}`**
  说明 Pages Functions 的边缘代理已经在工作（此前 `_routes.json` 无效，Functions 路由根本没生效）
- `/api/cg/ping` 403 是 CoinGecko 免费档自身限流，代码里未使用该端点（grep 命中的全是
  escaping/sniper/pingo/shaping 的误匹配），无需处理

## 3 个警告的定性

| 警告 | 性质 |
|---|---|
| `Node.js 20 is deprecated ... actions/checkout@v4, actions/setup-node@v4` | **外部噪音**。GitHub runner 升级 Node 24 导致所有仓库都出现，与本项目无关，无法在本地修复 |
| `the token reported no permission groups at all ...` | **我们自己的脚本产生的噪音**，已修复 |
| `access from this response. The next step verifies it against the real API.` | 同上 |

## 修复：健康的流水线必须是零警告

原因：`/user/tokens/verify` 对本站这种 **account 级 dashboard token** 返回空的
`permission_groups` 数组，所以"有没有 Pages 权限"这个提示永远答不了"是"。
当初设计成 warn 而非 fail 是对的（名字匹配只能是提示），但**每次都响的提示 = 噪音**，
结果就是一次成功的部署看起来像坏的，团队学会忽略 Annotations 面板，
真正重要的那条警告反而被漏掉。

改动（`scripts/verify-cloudflare-token.mjs`）：
- 新增 `note()` → 输出到步骤日志，**不进 Annotations 面板**
- 分支 3（无法判断 Pages 权限）从 `::warning::` 降级为 `note`
- 保留 warn 的只有**可行动**的情况：Pages 只读权限、账号不匹配、即将过期
- 信息没有丢失：下一步 `GET /accounts/{id}/pages/projects` 才是权威判断，401/403 硬失败

## 新增常驻回归测试（防止这个噪音复发）

- `scripts/mock-cloudflare-verify.mjs` —— 11 个 fixture，`MOCK_SCENARIO` 环境变量切换，
  覆写 `globalThis.fetch`，无网络无真实 token；导出 `SCENARIOS` 供完整性校验
- `scripts/test-cloudflare-token.mjs` —— 12 个用例，**断言退出码 + 精确警告数**，
  并额外检查"健康 token 必须零警告"；fixture 与用例互相校验防漂移
- `package.json` 加 `test:cloudflare-token` 并接进 `test` 链
- `deploy-pages.yml` 新增步骤 6「Check the token verifier still behaves」（发布前跑，约 1 秒）

结果：`ok-no-groups`（本站真实 token 的形态）现在 exit=0 / 0 warning / 3 note。
**做了变异测试**：把 `note()` 改回 `warn()` 后套件立刻失败并报 `HEALTHY TOKEN WARNS`，
还原后 12/12 通过 —— 证明守卫真的会拦，不是摆设。

## 踩坑：Node `--import` 的两条相反规则（Windows）

- `--import` 需要**模块描述符**：裸的 `C:/repo/mock.mjs` 会被解析成 scheme 为 `c:` 的 URL
  → `ERR_UNSUPPORTED_ESM_URL_SCHEME`。必须用 `pathToFileURL(p).href`
- **入口脚本需要文件系统路径**：给它 `file://` URL，Node 22 会当相对路径处理，
  报出莫名其妙的 `Cannot find module 'C:/repo/file:/C:/repo/scripts/verify.mjs'`

## ★ 重要发现：Pages 自带 Git 集成也是被同一个 bug 打挂的

时间线对上了：
- `5c5b60a` **2026-08-31 12:31:15** 引入 `public/_routes.json`（提交信息
  "perf: stop invoking Pages Functions on static routes"）
- Pages 自带构建器报 `Failed: an internal error occurred / Failed to publish assets`
  —— **从同一天开始**
- 这正是 Pages 用来报告 `_routes.json` 无效的方式；Wrangler 会直接说
  `Invalid _routes.json file found at: dist/_routes.json`

**推论已实证**：`ed1c0e2`（20:58:19）新增了 `public/logos/tokens/chevron-xstock.png`，
该提交之后 Wrangler 工作流**没有跑过**（它是 `workflow_dispatch` 纯手动），
而线上该资源返回 `200 / image/png / Content-Length: 4098`，与本地字节数完全一致，
且 `cf-cache-status: HIT`。对照实验：不存在的路径返回 404。
→ **Pages Git 集成确实已经自己构建并发布了，恢复正常。**

因此 workflow 顶部注释里"Pages 构建器平台故障 + 引用 workers-sdk#13259"的结论是错的，
已改写：保留原文但标注 `[SUPERSEDED]`，并写明它错在哪（循环论证：
"docs-only 提交 + dist 字节相同 ⇒ 与内容无关"  这句话默认了内容是无辜的）。

## 待用户决策（已写进 workflow 文件）

现在**每次 push 会被构建两次**（Pages 一次 + Wrangler 一次），二选一：

- **A（推荐）保留 Wrangler，在 Cloudflare 后台断开 Pages Git 集成**
  （Pages > 项目 > Settings > Builds & deployments > Disconnect），
  并取消注释 workflow 里的 `push: branches: [main]`。
  优点：错误可读 + 13 步预检 + 每次 push 只构建一次。
- **B 保留 Pages Git 集成，删掉这个 workflow**。零维护，但故障时又只剩
  "an internal error occurred"，没有诊断信息。

`push:` 触发器已注释好放在文件里，选 A 时直接取消注释即可。

## 提交

- `37a6e8b` Keep a working token from warning on every deploy（5 文件 +383）
- `337643f` Replace the wrong "Pages builder is broken" theory with what we found（+55/-14）
- 中途远程多了定时提交 `ed1c0e2`（刷新链上代币），按项目惯例 `fetch + merge FETCH_HEAD` 合并
- 注意：`.git/refs/remotes/origin/` 又被外部进程删掉一次（这个环境反复发生），
  表现是 `origin/main` 解析不了但 `git ls-remote` 正常；解法是在**一条命令里**连续
  fetch + 查看 + merge，缩短被删窗口
