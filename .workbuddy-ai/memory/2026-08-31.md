# 2026-08-31

## 全站审计（用户提问："看下网站还有哪些未完善的地方"）

### 已修复并上线

1. **`/api/cg/simple/price` 502 —— 确认是部署滚动期瞬时抖动，非代码问题**
   - 直连上游带 key：`/global` 200、`/simple/price` 200（`{"bitcoin":{"usd":77809}}`）
   - 线上代理 5 个端点全部 200，`X-CG-Auth: demo-key` 生效
   - 用组件真实请求串复测：TickerBar 29 币 200、dashboard per_page=100 200
   - 教训：`/tmp` 在本沙箱不可写，curl exit 23 会造成**假阴性**。探测脚本必须写项目目录内。

2. **死链 `/category/exchanges`（8 篇 learn 文章 CTA 全中招）** — commit `c686b20`
   - `src/pages/learn/[slug].astro:130` 硬编码 `/category/exchanges`（复数），实际 id 是单数
   - 文案也错：用 `article.tags[0]`（"staking"/"pos" 等自由标签）冒充目录分类
   - 修法：给 8 篇文章加 `directoryCategory`（合法分类 id），CTA 按文章主题跳对应分类
     defi×3 / infra×2 / market×2 / analytics-trading×1
   - 注意：`article.category` 是**显示名**（"DeFi"/"Education"），供徽章和相关文章分组用，不可当 id 用

3. **`.gitignore` 补 `.dev.vars`** — commit `1946527`

### 审计方法（可复用）

爬 sitemap 全站 → 抽所有站内 `href` → 逐个验状态码。比"猜 URL 再测"可靠得多：
猜 URL 会产出大量假死链（`/project/*`、子分类页本就不存在，属设计如此）。
本次 44 页 → 45 个内部链接 → 只有 1 个真 404。
注意排除 `/cdn-cgi/l/email-protection`（Cloudflare 邮箱混淆，非真链接）。
308 = Cloudflare Pages 尾斜杠重定向，正常，浏览器会自动跟随。

### 站点现状（44 个 sitemap URL / 45 页）

- 63 项目、24 个分类、10 条链、500 条链上代币（10 链 × 50）
- SEO 元数据齐全（title/description/canonical/OG/Twitter 全有，仅 `/admin` 无，正常）
- `og-image.png` 存在且线上 200；robots.txt 带 sitemap；Cloudflare Web Analytics 已装
- `/api/auth` 存在返回 200（Decap CMS GitHub OAuth 正常）

### 待办（本次未做，已向用户报告）

**需要用户操作（非代码）**
- `src/data/affiliates.json`：8 个交易所 `code` 全空 → 联盟链接未激活（变现第一优先）
- `src/data/ad-network.json`：8 个广告位 `html` 全空 → 仍显示 "Your Ad Here" 占位
- GSC 重新提交 sitemap（现 44 URL）

**可由我完成（代码/数据）**
- 63 项目中 **48 个缺 `chains` 字段** → 卡片无链徽章；且 `getProjectsByChain()` 写好但
  **从未被调用**，链页只有 CoinGecko 代币、没有该链精选项目（SEO 价值损失）
- **全站 0 个 JSON-LD 结构化数据**（Organization/WebSite/BreadcrumbList/Article）
- 无 FAQ 页、无 Newsletter 订阅

## 用户确认后完成的三项（同日下午）

用户指令："ad-network.json 请用主流的行业内站点替代。chains字段请补充。FAQ和Newsletter订阅页面完成建立，订阅最好是用邮件订阅。"

### 1. 广告位改用主流行业站点（commit `febb5bf`）

不再等 Coinzilla/Bitmedia 的 tag，8 个广告位各自推广一个已在 projects.json 收录的
主流站点（Bybit / Trezor / OKX / CoinMarketCap / Magic Eden / DexScreener / Kraken / Coinbase）。

**关键设计**：推广链接走 `getReferralUrl(project)`，所以用户一旦填了 affiliates.json
的 code，8 个广告位自动变成联盟链接，无需再改代码。

优先级：直投广告(ads.json) > 广告网络 html tag > 主流站点推广 > Your Ad Here 占位。
保留 `html` 字段，将来接真广告网络直接填即可。

新增 `getNetworkPromo(slot)` + `NetworkPromo` 类型 + AdBanner 渲染分支。
全站 `__ placeholder__` 归零。脚本 `scripts/check-ad-slots.mjs` 可复查。

### 2. chains 字段补全（同 commit）

补 38 个（15 个原本就有）。**10 个有意留空**：新闻媒体（cointelegraph/theblock/
coindesk/tokeninsight）、审计（openzeppelin/certik）、安全工具（rekt）、税务（koinly）、
空投日历（airdropsio/coinmarketcal）——它们不部署在链上，加链徽章是错数据不是好数据。

链页接入 `getProjectsByChain()`（此前写好却从未调用），新增
"Best {chain} Projects & Tools" 区块，卡片带联盟链接，内容+变现双收。
分布：ethereum 47 / arbitrum 38 / polygon 38 / bsc 37 / base 33 / optimism 30 /
solana 27 / avalanche 19 / sui 7 / ton 6。链页 HTML 176KB→295KB（Brotli 后约 35KB）。

补数据脚本：`scripts/fill-project-chains.mjs`（含合法链 id 校验，非法即中断）。

### 3. FAQ + Newsletter（commit `e18ec19`）

- `/faq`：24 问 5 组，带 **FAQPage JSON-LD**——这是全站第一个结构化数据
- `/newsletter`：**真邮件订阅**，后端是新 Pages Function `POST /api/subscribe`
  - env：`NEWSLETTER_PROVIDER`(generic|mailchimp|buttondown) /
    `NEWSLETTER_ENDPOINT` / `NEWSLETTER_TOKEN`(Secret)
  - 自己不发邮件：Pages 无出站 SMTP，能向任意地址发信的端点等于开放中继
  - 未配置 → 503 `{unconfigured:true}`，前端自动显示 mailto 兜底，不会静默失败
  - 蜜罐字段防机器人；邮箱小写化+trim；上游 400 视为"已订阅"按成功处理
  - `SubscribeForm.astro` 组件复用，页脚也挂了，全站可订阅
- 测试：`test-subscribe.mjs` 33 条 / `check-faq-newsletter.mjs` 20 条
  / `check-live-links.mjs`（爬 sitemap 全站查死链，46 URL 零死链）
- `npm test` 现在串起：代理 28 + 订阅 33 + build + FAQ/Newsletter 20 + 广告位

### 页面数：44 → 46（sitemap）/ 45 → 48（build，含 404 与 admin）

### 仍然待办
- affiliates.json 的 8 个 code（**现在价值更高了**：广告位和链页卡片都走它）
- GSC 重交 sitemap（46 URL）

---

## 4. 全站 JSON-LD 结构化数据（commit `9f80416`）

### 架构：中央工厂 + 组件 + Layout 注入

- `src/utils/schema.ts`：所有 schema.org 节点的唯一来源
  （`organization` / `webSite` / `breadcrumbList` / `itemList` /
  `collectionPage` / `article` / `faqPage`，以及 `abs()` 相对路径转绝对）
- `src/components/JsonLd.astro`：多个节点自动折成一个 `@graph`，
  只输出一个 `<script>`、一个 `@context`；**转义 `< > &`** 防提交内容截断脚本标签
- `Layout.astro` 新增两个 prop：`breadcrumbs?: Crumb[]` 与 `schema?: unknown`
  → 传了面包屑就自动出 `BreadcrumbList`，全站继承

### 覆盖（线上已验证，17 条路由逐个 curl 确认 `@type`）

| 页面 | 节点 |
|---|---|
| `/` | Organization + WebSite + ItemList |
| `/category/*` | CollectionPage + ItemList + Breadcrumb |
| `/chain/*` | CollectionPage + ItemList(前30代币) + ItemList(项目) |
| `/learn/*` | Article + Breadcrumb |
| `/learn` `/compare` | CollectionPage + ItemList + Breadcrumb |
| `/compare/*` | Article + Breadcrumb |
| `/airdrops` | CollectionPage + ItemList（**只收 confirmed/ongoing**） |
| `/unlocks` | CollectionPage + ItemList（**只收 upcoming**） |
| `/faq` | FAQPage（改为走统一 helper）+ Breadcrumb |
| 静态页 | Breadcrumb（about/privacy/terms/submit/advertise/newsletter/dashboard） |

`dist` 统计：BreadcrumbList 45 页 / ItemList 25 / CollectionPage 24 /
Article 13 / FAQPage 1 / Organization 1 / WebSite 1

### 两个有意的取舍（别改回去）

1. **WebSite 不挂 `SearchAction`**：Header 搜索是客户端 Fuse.js，
   没有服务端渲染的 `/search?q=` URL。挂上去等于指向一个答不了查询的页面，
   属于结构化数据违规。等真有搜索结果页再补（schema.ts 里已写注释）。
2. **目录类 ItemList 的 url 指向外部站点**：本站**没有 `/project/*` 详情页**，
   写站内路径就是死链。`check-jsonld.mjs` 有一条断言专门防这个。

### 测试

- 新增 `scripts/check-jsonld.mjs`，**3268 条断言**，扫 `dist/**/index.html`：
  JSON 可解析 / 无 `</script` 越界 / Article 必填字段 / URL 必须绝对 /
  面包屑 position 连续且末项无 item / ItemList 非空且 numberOfItems 一致 /
  内部 URL 必须真实存在 / Organization 全站只出现一次
- 已接入 `npm test`（代理 28 + 订阅 33 + build + FAQ20 + 广告位 + JSON-LD）
- **坑**：`check-faq-newsletter.mjs` 原来直接读 `ld.mainEntity`，FAQ 改走
  `@graph` 后抛 TypeError 被 catch 吞掉——暴露出它那条
  `faq answers have text` 断言**从来没被执行过**。已改为在 `@graph` 里找
  FAQPage 节点，现在 20/20（之前是 19）。

### 数据改动
- `comparisons.json` 5 条各加 `publishedAt`/`updatedAt`（Article 需要真实日期）
  —— 注意：**别用 node 重写整个 JSON**，会把紧凑数组全部展开成 347 行噪音，
  用精确 Edit 插入，最终 diff 只有 10 行

### 线上状态
- 46 个 sitemap URL、46 条内链、**0 死链**
- 17 条代表性路由全部 200 且 `@type` 正确

---

## 5. 全站诊断 + 竞品分析 + 突围方案（产出 `CryptoNav-网站诊断与流量突围方案.md`）

### 实测发现（全部可复现，不是主观感受）

**内容结构**
- 46 页 / 38,038 词 / **中位 584 词** —— 低于 2026-06 核心更新后的「150 页主题深度」红线
  （该更新后 <150 页单主题站出现类目级崩塌，200-2000 页中型站掉 40-70%，权威站 +15-35%）
- **9 个分类页只有 279-392 词**（商业意图最强的钱页最薄），而 8 篇 learn 文章
  1405-1548 词（最没商业价值的内容最厚）——**内容投入与商业价值完全倒挂**
- **一手经验信号 0/46 页** —— YMYL 领域最致命。有一手经验信号 +38% 可见度，
  纯 listicle -63%

**技术**
- `cf-cache-status: DYNAMIC`，HTML **从不走 CDN 缓存**（连测 3 次）
  - 根因（Cloudflare 官方原文）：「一旦 Pages 项目加了 Functions，**默认所有请求都会调用
    Function**」。项目有 5 个 Functions 却没有 `_routes.json`
  - 已补 `public/_routes.json`（`include:["/api/*"]`）+ `public/_headers`，commit `5c5b60a`
  - **线上响应头仍未变化**，需在面板 Caching → Cache Rules 手动加一条
- 链页 **4 词/KB**（250-312KB HTML 承载 ~1100 词）；home 132KB
- header 14.9KB / **52 个链接**，占 learn 整页 32%（桌面+移动两套导航都写进 HTML）
- `auto.CcLMyVVA.js`（Chart.js）204KB 打进首页首屏

**SEO 基础面**
- **内链图完全平的**：46 个页面每一个都链向全部 9 个分类（header 硬编码），
  无 hub-and-spoke，权重无法集中；`/chain/ton` 内链数为 0
- 9 个 title >65 字符（SERP 截断）、6 个 <25 字符；5 个页面共用 Layout 默认 description
- robots.txt 是 **Cloudflare 托管版**，屏蔽了 GPTBot / ClaudeBot / Google-Extended /
  CCBot / Applebot-Extended / Bytespider（**战略取舍，待用户决策**）

### 竞品格局（关键洞察）

- **纯目录形态已被证伪**：DappRadar 2025-11 关停（7 年、2400 万 DAW、
  被机构引用，官方原话 "financially unsustainable"）。Cryptwerk 已腐化，
  唯一像样的直接对手是 CryptoLinks（单人运营，靠 **1,219 条骗局名单** 立身）
- **巨头不是靠写内容赢的**：DefiLlama / DexScreener 的自然流量几乎全是
  **品牌/导航词**，非品牌词接近零 —— 是**产品赢、SEO 收租**
- **唯一被验证的冷启动路径 = 可嵌入**：DefiLlama 头部外链来自 GitHub、
  Bitget 钱包、Morpho 官网、Tronscan；DexScreener 38K 引用域大量来自
  「每个代币页自动挂 View on DexScreener」。**可嵌入 = 外链飞轮**
- 巨头共同盲区：跨站对比、"best X for [场景]"、"Is X safe/rug/legit"、
  国别+监管、**任何需要编辑判断的内容**
- 行业正在集体失血：CoinGecko 43.5M→18.5M，CMC 157M→64M，Cointelegraph -99.9%，
  2025-09 前 77% 头部加密媒体失流

### 突围方案（一个主攻点，不铺七个方向）

**把「收录」升级为「核验」（Safety Verification）** —— 做第一个敢给判定结果的目录站。
8 个维度自动核验 63 个项目：域名年龄(WHOIS) / 合约是否可任意增发 / 蜜罐检测 /
审计情况 / 团队可验证性 / 历史安全事件 / 流动性深度 / 仿冒站检测。
产出 `/verify/[slug]` + Safety Score + 可嵌入徽章。

选它的四个理由（缺一不可）：
1. 巨头不敢做（定性判断有法律/商誉风险）→ 结构性缺口，不是暂时窗口
2. 真实搜索需求（"is X legit" 类高意图长尾）
3. 一次性解决两个致命伤：核验字段**就是专有数据**（被引用理由），
   核验过程**就是一手经验信号**（YMYL 的 E-E-A-T 门槛）
4. 可自动化，正好匹配已有的采集基建，边际成本极低

依赖顺序不能跳：核验数据 → 可嵌入徽章换外链 → 铺到 200+ 页 → 12-18 个月后谈流量

**立刻停止**：继续写通用百科内容。现有 8 篇 learn 文章在跟 CoinGecko Learn /
Kraken Learn / Coinbase Learn（DA 90+）抢词，必输，且占用了本该投入核验体系的产能。

### 时间预期（说真话）
- 6-12 个月起量不现实，**12-18 个月是基准**，且前提是 6 个月内走完上面那条链
- 核心更新受击后前 3 个月只改善 0-15%
- 含专有数据的内容被 AI 引用率 2 倍；**只改标题年份不改实质内容会被降权**
- 🔴 未找到任何可信的 2025-26 年"小团队 crypto 站 6-12 个月起量"公开案例
  （检索结果全是 SEO 代理商软文）

### AI 爬虫取舍（待用户决策）
- 被 AI Overview 引用的站点比未被引用的**多 35% 点击**，**93.8% 的 AIO 引用
  来自传统首页之外** → 对 DA=0 新站，AI 答案是少数还能「被引用而非排名」的渠道
- `Google-Extended` 管的是 Gemini 与模型训练，**不影响** Google 搜索的 AI Overviews 收录
- 建议：至少放开 PerplexityBot 和 GPTBot，保留 `ai-train=no`

## 突围执行（一）：核验管线落地 — 已推送 180c73b

用户指令「那按照你诊断，如何突围，请帮我突围」→ 执行诊断中的突围点：
把目录站改造成**可核验层**。

### 交付
- `scripts/build-safety-report.mjs` 核验管线（4 个免费无 key 数据源）
- `scripts/curation.mjs` 人工策展映射（含 REJECTED_MATCHES 显式记录拒配）
- `scripts/check-safety.mjs` 1298 条断言（已接入 `npm test`）
- `src/data/safety.json` 63 个项目真实核验数据
- `/verify`（索引）+ `/verify/[slug]`（63 页）+ `/embed/[slug]`（63 个 2.3KB 徽章）
- `SafetyBadge` 组件接入 ProjectCard / 首页 / Header / Footer
- `.github/workflows/refresh-safety.yml` 每周一 07:00 UTC 刷新
- 页面总数 45 → 174

### 数据源实测结论（反复踩坑得出，别再试错）
1. **RDAP**：IANA 引导文件**不含 .io / .co**。.io 走 `rdap.identitydigital.services/rdap/`
   可通；.co 全端点不可达（theblock.co 因此永久无域名年龄）。Verisign 会限流，需重试。
2. **GoPlus 免费档一次请求只返回 1 个地址**，传 15 个也只回第一个 → 批量请求会**静默丢弃**。
   必须逐地址请求。
3. **DefiLlama /hacks 的 `defillamaId` 在 CEX/钱包类事件上大量缺失**（Binance $570M、
   Kraken $10.4M、Bitget $100M 全无 ID），且协议按版本拆 ID（Aave V1–V4 是 4 个）。
   只能人工策展归属，但**金额/日期全部实时取自源数据**。
4. **Wayback CDX 在沙箱代理下不可达**（web.archive.org 被白名单拦截），CI 里应可用。
5. 沙箱代理只放行部分 host：binance.com / aave.com / uniswap.org 均 `fetch failed`，
   这是**环境限制不是站点问题**，探针要用 SKIP 而非 FAIL 区分。

### 三个致命陷阱（都是"数据看起来对但实际会毁掉产品"）
1. **域名注册时间 ≠ 项目年龄**。solana.com=1993、base.org=1996、bybit.com=1999、
   ledger.com=1994、dune.com=1997、aave.com=1997、sushi.com=1999 — 全是二级市场买的
   溢价老域名。直接拿来算年龄 = 公开写「Solana 运营 33 年」。
   → 对策：以 Wayback 首次抓取为准；无 Wayback 且注册日早于 2008-10-31（比特币白皮书）
   时，`operatingSince` 置 null（不发布运营年龄）+ 分数打 0.4 折 + 页面显示警告。
   **域名注册日期仍作为事实展示，但标注它不是项目年龄。**
2. **DefiLlama 文本匹配会诬告**。"Blur Finance"（BSC 跑路盘）≠ Blur.io、
   "Phantom Galaxies"（NFT 游戏）≠ Phantom 钱包、"Safe Dollar" ≠ Safe{Gnosis}、
   "Solido Cash" ≠ Lido、"RocketSwap/Grand Base" ≠ Coinbase 的 Base、
   "AVE" 撞 Aave/Agave/xAVE/Save。→ 人工策展 + REJECTED_MATCHES 作为数据发布。
3. **低覆盖度反而拿满分**。MEXC 只有 1 个维度有数据，却因「无事件」得 100 分。
   → 二维度门槛（<2 个维度不评分）+ **分数封顶于覆盖度**（只验了 75% 的评分项，
   最多只能得 75 分）。封顶前 17 个项目并列 100 分，封顶后分布 58–98。
   「我们明明写着这不是审计，却给满分」是最不能犯的错。

### 评分模型
- longevity 35% / incidents 40% / contract 25%
- 缺失维度**权重按比例重分配**（无代币的 CEX 不因没有代币被扣分）
- 维度分先归一化到 0–100 再加权（曾漏掉这步，导致满分只有 32.7）
- 事故惩罚 = 严重度(按金额分档) × 时间衰减(1年内1.0 / 1-3年0.8 / 3-5年0.6 / >5年0.4)
- 结果：A 11 个（全是 100% 覆盖的代币项目）/ B 44 / C 7 / 未评级 1（The Block）
- 垫底：Bybit 58（史上最大 $14 亿黑客）、Bitget 58、Kraken 63、OKX 66 — 符合事实

### 管线性能教训
- 初版 9 分钟跑不完：对网络错误做 3 次重试 × 25s 超时 × 63 域名。
  → **网络层错误（含 TimeoutError）不重试** + 熔断器（连续 3 次失败即跳过该源）→ 22 秒。
- 无状态重跑会**丢已有数据**（上一轮成功的 pancakeswap/arbitrum 下一轮失败）。
  → 按不可变性缓存：RDAP 永久缓存，GoPlus 7 天，Wayback 30 天，hacks 每次拉新。

### 待办（下一步）
- CI 首次跑通后确认 Wayback 生效（本地无法验证），届时 15 个「老域名」项目
  会从 `low` 置信度升为 `high`，分数会上升
- arkham 的 GoPlus 始终 no-result，合约地址待复核
- 徽章外链飞轮需主动 BD：联系项目方嵌入 /embed/[slug]
- 用户仍有旧待办：affiliates.json 8 个 code 字段、Cloudflare 缓存规则（_headers 未生效）、
  Newsletter 环境变量、AI 爬虫放开决策
